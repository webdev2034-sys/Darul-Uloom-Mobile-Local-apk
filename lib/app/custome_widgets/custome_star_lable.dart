import 'package:flutter/material.dart';

class CustomeStarLable extends StatelessWidget {
  const CustomeStarLable({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(' *', style: TextStyle(color: Colors.red, fontSize: 16));
  }
}
